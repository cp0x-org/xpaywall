# xpaywall

Repository layout:

- [`proxy/`](xgateway) - standalone x402 reverse proxy project
- [`control-api/`](./control-api) - standalone control API project

The two Go services are split into separate project roots and do not share code directories.
