# Copy to .env and fill in. Both vars are required.

# Tempo JSON-RPC endpoint. Must be the same chain the xgateway route charges on.
RPC_URL=https://rpc.moderato.tempo.xyz

# Payer wallet private key (hex). Must be funded on the chain above.
PRIVATE_KEY=0x...
