services:
  proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xpaywall-proxy
    ports:
      - "8080:8080"
    environment:
      PROXY_CONFIG: /app/config.yaml
    volumes:
      - ./config.example.yaml:/app/config.yaml:ro
    restart: unless-stopped
